# API Configuration
# Port exposed to host
API_PORT=8000
# Env vars for API Application

# Database connection string
# ⚠️ For Docker containers: Use host.docker.internal (macOS/Windows) or host IP instead of localhost
# Example: postgresql://user:password@host.docker.internal:5432/database
# Or use Docker service name if database is in another container: postgresql://user:password@postgres:5432/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/caxa-chat

# Web Configuration
# Port exposed to host
WEB_PORT=3000
# Build-time variables for Next.js (Must be set during build)
NEXT_PUBLIC_API_URL=http://localhost:8000

